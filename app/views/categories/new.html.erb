<h1 class="icon-profile"><span>New Category</span></h1>
<div class="content-profile">
  <div id="formDiv">
    <%= form_for @category, :url => categories_path, :method => 'POST' do |f| %>
      <%= render :partial => 'form', :locals => {:f => f} %>
    <% end %>
  </div>